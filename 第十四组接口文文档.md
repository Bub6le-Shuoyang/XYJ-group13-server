# 第十四组接口文文档

# **乡驿家 \(XiangYiJia\) 服务端 — 核心业务 API 文档**

## **一、全局约定**

### **1\.1 基础路径**

所有 RESTful API 以 `/api/v1` 为版本前缀。

### **1\.2 统一响应格式**

所有接口返回 `Result\&lt;T\&gt;` 结构：

*代码块*

```Plain Text
{
  "code": 200,
  "message": "success",
  "data": {},
  "timestamp": 1745784000000
}
```

### **1\.3 认证方式**

除登录接口外，所有业务接口均使用 **JWT Token** 认证，通过请求头 `Authorization: Bearer \&lt;token\&gt;` 传递。

当前移动端只支持账号密码登录，不接入图形验证码、邮箱验证码、短信验证码或第三方登录。

### **1\.4 当前业务范围**

当前移动端只支持收件/上门配送业务，不接入寄件主流程。历史寄件接口可保留为后端预留能力，但移动端不展示入口、不主动调用。

### **1\.5 参数校验**

所有 `@RequestBody` 参数均使用 `@Validated` 注解触发 Jakarta Validation 校验，校验失败会返回字段级错误信息。

### **1\.6 分页响应格式**

列表接口统一返回 `Page\&lt;T\&gt;`：

*代码块*

```Plain Text
{
  "records": [],
  "total": 100,
  "size": 10,
  "current": 1,
  "pages": 10
}
```

**\-\-\-**

## **二、接口总览**

> 另有 `AdminController`（`/admin`）提供页面路由（login、index），返回视图名，非 RESTful API。
> 
> 

**\-\-\-**

## **三、系统配置与广告模块 \(****`SysConfigController`****\)**

路径前缀：`/api/v1/sys`

### **3\.1 获取随机开屏广告**

*代码块*

```Plain Text
GET /api/v1/sys/ads/splash
```

**认证**：无需认证

**业务逻辑与方案说明**：
1. **获取时机**：App 启动或在后台静默时，主动拉取此接口。
2. **随机策略**：后端会查出所有状态为上线（status=1）的开屏广告，并根据 `weight`（权重）字段进行随机抽取，权重越大的广告越容易被抽中。接口只返回抽中那 **1** 条广告数据。
3. **缓存与展示**：前端拿到该广告的 `image_url` 后，将其图片文件下载并缓存在本地。当用户**下一次**冷启动 App 时，直接读取本地缓存的广告图片进行秒开展示；用户点击图片时跳转到对应的 `target_url`。

**响应 data**：`SplashAdVO`

*代码块*

```Plain Text
{
  "ad_no": "AD_SPLASH_001",
  "name": "乡驿家助农大促",
  "image_url": "/admin/picture/ad_splash_1.png",
  "target_url": "https://xiangyijia.com/promo1"
}
```

**\-\-\-**

## **四、统一认证模块 \(****`AuthController`****\)**

路径前缀：`/api/v1/auth`

### **3\.1 账号密码登录**

*代码块*

```Plain Text
POST /api/v1/auth/login
```

**认证**：无需认证

**说明**：移动端推荐使用该接口统一处理村民、站点管理员、骑手三类角色登录。当前只支持账号密码登录。

**请求体**：`LoginDTO`

*代码块*

```Plain Text
{
  "account": "admin@example.com",
  "password": "MyPass123!",
  "role": "ADMIN"
}
```

**响应 data**：`LoginResponseVO`

*代码块*

```Plain Text
{
  "token": "eyJhbGciOiJIUzI1NiJ9...",
  "refresh_token": "eyJhbGciOiJIUzI1NiJ9...",
  "expires_in": 7200,
  "user": {
    "id": 1,
    "user_no": "U20260524001",
    "account": "admin@example.com",
    "email": "admin@example.com",
    "phone": "13800138000",
    "nickname": "站点管理员",
    "avatar_url": "/uploads/avatar.png",
    "role": "ADMIN",
    "is_realname_auth": true
  }
}
```

### **3\.2 获取图形验证码**

*代码块*

```Plain Text
GET /api/v1/auth/captcha
```

**认证**：无需认证

**响应 data**：`CaptchaResponseVO`

*代码块*

```Plain Text
{
  "captcha_id": "cpt_123456789",
  "captcha_image_base64": "data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAA..."
}
```

### **3\.3 发送邮箱验证码**

*代码块*

```Plain Text
POST /api/v1/auth/send-email-code
```

**认证**：无需认证

**说明**：发送验证码前，需通过图形验证码校验，防止恶意刷接口。

**请求体**：`SendEmailCodeDTO`

*代码块*

```Plain Text
{
  "email": "user@example.com",
  "captcha_id": "cpt_123456789",
  "captcha_code": "abcd"
}
```

**响应 data**：`Boolean`

### **3\.4 统一注册接口**

*代码块*

```Plain Text
POST /api/v1/auth/register
```

**认证**：无需认证

**说明**：支持普通村民（USER）、骑手（COURIER）及管理员（ADMIN）的统一注册。必须校验邮箱验证码，并包含密码二次确认。通过 `role` 字段区分注册身份，后端分别插入对应的数据库表（`users` / `couriers` / `admins`）。

**请求体**：`RegisterDTO`

*代码块*

```Plain Text
{
  "email": "user@example.com",
  "email_code": "123456",
  "password": "MyPass123!",
  "confirm_password": "MyPass123!",
  "role": "USER"
}
```

**说明：**
- `role` 可选值：`USER` (普通用户)、`COURIER` (骑手)、`ADMIN` (管理员)
- `confirm_password` 必须与 `password` 一致。
- 注册时后端将根据邮箱前缀自动初始化用户昵称或骑手名称。

**响应 data**：`LoginResponseVO` (注册成功后自动登录，返回 Token 信息，结构同 3\.1 登录接口)

### **3\.5 获取当前登录账号**

*代码块*

```Plain Text
GET /api/v1/auth/me
```

**认证**：需要 Bearer Token

**响应 data**：`UserVO`

### **3.6 刷新 Token**

*代码块*

```Plain Text
POST /api/v1/auth/refresh
```

**认证**：无需 Bearer Token

**请求体**：

*代码块*

```Plain Text
{
  "refresh_token": "eyJhbGciOiJIUzI1NiJ9..."
}
```

**响应 data**：`LoginResponseVO`

### **3.7 退出登录**

*代码块*

```Plain Text
POST /api/v1/auth/logout
```

**认证**：需要 Bearer Token

**响应 data**：`Boolean`

**\-\-\-**

## **四、管理员认证兼容模块 \(****`AdminAuthController`****\)**

路径前缀：`/api/v1/admin/auth`

> 本模块为后台已实现接口。移动端只需要兼容账号密码登录，不使用图形验证码、邮箱验证码、注册和重置密码流程。
> 
> 

### **4\.1 获取图形验证码（后台预留，移动端不使用）**

*代码块*

```Plain Text
GET /api/v1/admin/auth/captcha
```

**认证**：无需认证

**响应 data**：`CaptchaResponseVO`

### **4\.2 发送邮箱验证码（后台预留，移动端不使用）**

*代码块*

```Plain Text
POST /api/v1/admin/auth/send-email-code
```

**认证**：无需认证

**请求体**：`SendEmailCodeDTO`

**响应 data**：`SendCaptchaResponseVO`

### **4\.3 管理员注册（后台预留，移动端不使用）**

*代码块*

```Plain Text
POST /api/v1/admin/auth/register
```

**认证**：无需认证

**请求体**：`AdminRegisterDTO`

**响应 data**：`LoginResponseVO`

### **4\.4 管理员账号密码登录**

*代码块*

```Plain Text
POST /api/v1/admin/auth/login
```

**认证**：无需认证

**说明**：兼容已实现后台管理员登录接口。移动端管理员登录可以临时使用该接口，但推荐后续统一到 `/api/v1/auth/login`。

**请求体**：`AdminLoginDTO`

*代码块*

```Plain Text
{
  "email": "admin@example.com",
  "password": "MyPass123!"
}
```

**响应 data**：`LoginResponseVO`

### **4\.5 重置密码（后台预留，移动端不使用）**

*代码块*

```Plain Text
POST /api/v1/admin/auth/reset-password
```

**认证**：无需认证

**请求体**：`AdminResetPasswordDTO`

**响应 data**：`null`

**\-\-\-**

## **五、管理员资料模块 \(****`AdminProfileController`****\)**

路径前缀：`/api/v1/admin/profile` \| **需要认证**

### **5\.1 获取当前管理员信息**

*代码块*

```Plain Text
GET /api/v1/admin/profile
```

**认证**：需要 Bearer Token

**响应 data**：`Admins`（`passwordHash` 字段置空）

### **5\.2 更新当前管理员资料**

*代码块*

```Plain Text
PUT /api/v1/admin/profile
```

**认证**：需要 Bearer Token

**请求体**：`Admins`（部分更新）

*代码块*

```Plain Text
{
  "realName": "新姓名",
  "phone": "13900139000",
  "avatarUrl": "/uploads/new_avatar.png"
}
```

**响应 data**：更新后的 `Admins`（`passwordHash` 字段置空）

**\-\-\-**

## **六、用户包裹模块 \(****`UserPackageController`****\)**

路径前缀：`/api/v1/user/packages` \| **需要认证，角色 USER**

### **6\.1 获取我的收件列表**

*代码块*

```Plain Text
GET /api/v1/user/packages?type=RECEIVE&page=1&size=10
```

**说明**：当前移动端只传 `RECEIVE`。`SEND` 为历史预留，不作为 App 主流程。当前实现只返回 `packages.receiver_user_id = 当前登录用户ID` 的包裹；新注册用户若未绑定包裹，应返回空列表，不能返回测试数据或其他用户包裹。

**响应 data**：`Page\&lt;PackageVO\&gt;`

*代码块*

```Plain Text
{
  "records": [
    {
      "package_id": "PKG-003",
      "task_id": "TASK-003",
      "name": "药品快件",
      "receiver_name": "赵奶奶",
      "receiver_phone": "13800001111",
      "address": "清河村卫生室旁",
      "weight": 1.2,
      "estimated_fee": 0,
      "status": "ASSIGNED",
      "pickup_code": "QJ25003",
      "sender_name": "县医院",
      "reward_amount": 12,
      "timeline": ["县医院已发出", "站点完成入库", "张师傅正在送货上门"],
      "courier_name": "张师傅",
      "station_id": "ST-001",
      "station_name": "清河村中心驿站",
      "lat": 30.503,
      "lng": 114.302
    }
  ],
  "total": 1,
  "size": 10,
  "current": 1,
  "pages": 1
}
```

### **6\.2 用户提交包裹信息**

*代码块*

```Plain Text
POST /api/v1/user/packages
```

**说明**：用户提交包裹信息后，服务端写入 `packages` 表，初始状态为 `PENDING_INBOUND`（移动端展示为“待审批”）。此时骑手不可抢单，必须等待站点管理员审批。移动端应先调用 `GET /api/v1/stations/nearby` 获取数据库中的驿站列表，用户选择寄件驿站后提交 `station_id`。`order_no` 会作为 `packages.package_no` 保存，寄件方由服务端根据 `station_id` 读取站点名称，不允许前端手填。

**请求体**：

*代码块*

```Plain Text
{
  "order_no": "JDV000123456789",
  "station_id": 5,
  "receiver_name": "李爷爷",
  "receiver_phone": "13900002222",
  "address": "北京交通大学东区 8 号楼",
  "reward_amount": 8.0
}
```

**响应 data**：`PackageVO`

### **6\.3 获取包裹详情**

*代码块*

```Plain Text
GET /api/v1/user/packages/{package_id}
```

**响应 data**：`PackageVO`

**说明**：只能查看当前登录用户自己的包裹。若 `package_id` 不存在或不属于当前用户，返回业务错误。

### **6\.4 用户确认签收**

*代码块*

```Plain Text
POST /api/v1/user/packages/{package_id}/confirm
```

**说明**：用于用户主动确认签收场景。骑手上门核验取件码完成签收应使用 `POST /api/v1/courier/tasks/\{task\_id\}/verify\-pickup\-code`。

**响应 data**：`Boolean`

### **6\.5 评价包裹服务**

*代码块*

```Plain Text
POST /api/v1/user/packages/{package_id}/rate
```

**请求体**：

*代码块*

```Plain Text
{
  "score": 5,
  "comment": "配送非常快，态度很好！"
}
```

**响应 data**：`Boolean`

### **6\.6 提交投诉**

*代码块*

```Plain Text
POST /api/v1/user/packages/{package_id}/complain
```

**请求体**：

*代码块*

```Plain Text
{
  "reason": "包裹破损",
  "description": "外包装有明显水渍",
  "images": ["/uploads/img1.jpg"]
}
```

**响应 data**：`Boolean`

**\-\-\-**

## **七、用户个人中心模块 \(****`UserProfileController`****\)**

路径前缀：`/api/v1/user` \| **需要认证**

### **7\.1 获取用户资料首页**

*代码块*

```Plain Text
GET /api/v1/user/profile
```

**说明**：返回个人中心首页聚合数据，保证移动端首屏一次请求可展示。

**响应 data**：`UserProfileVO`

### **7\.2 获取常用地址列表**

*代码块*

```Plain Text
GET /api/v1/user/addresses
```

**响应 data**：`List\&lt;AddressVO\&gt;`

### **7\.3 新增常用地址**

*代码块*

```Plain Text
POST /api/v1/user/addresses
```

**请求体**：

*代码块*

```Plain Text
{
  "name": "张三",
  "phone": "13800001111",
  "address": "清河村 5 组 9 号",
  "is_default": false
}
```

**响应 data**：`AddressVO`

### **7\.4 获取优惠券列表**

*代码块*

```Plain Text
GET /api/v1/user/coupons?status=AVAILABLE&page=1&size=10
```

**响应 data**：`Page\&lt;CouponVO\&gt;`

### **7\.5 获取钱包流水**

*代码块*

```Plain Text
GET /api/v1/user/wallet/transactions?page=1&size=10
```

**响应 data**：`Page\&lt;WalletTransactionVO\&gt;`

### **7\.6 获取积分商城权益**

*代码块*

```Plain Text
GET /api/v1/user/mall/items?page=1&size=20
```

**响应 data**：`Page\&lt;MallItemVO\&gt;`

**补充说明：MallItemVO 模型包含如下字段：**
- `id`: 商品主键
- `name`: 商品名称
- `description`: 商品说明
- `type`: 商品类型，`coupon`=优惠券，`goods`=实物
- `image_url`: 展示图片链接，例如 `/admin/picture/小锅.png`
- `points_required`: 兑换所需积分
- `stock`: 剩余库存

### **7\.7 兑换积分商城权益**

*代码块*

```Plain Text
POST /api/v1/user/mall/items/{item_id}/redeem
```

**响应 data**：`RedeemRecordVO`

**说明**：兑换成功后服务端会扣减 `user_points_accounts.points` 和 `mall_items.stock`。如果兑换的是优惠券类型权益，会同步增加 `user_points_accounts.coupon_count`。响应中的 `remain_points` 可用于移动端立即刷新可用积分。

### **7\.8 获取兑换记录**

*代码块*

```Plain Text
GET /api/v1/user/mall/redeem-records?page=1&size=10
```

**响应 data**：`Page\&lt;RedeemRecordVO\&gt;`

**补充说明：RedeemRecordVO 模型包含如下字段：**
- `id`: 兑换记录主键
- `item_name`: 兑换权益名称
- `points_cost`: 消耗积分
- `remain_points`: 兑换后剩余积分
- `redeem_time`: 兑换时间
- `status`: 兑换状态

### **7\.9 获取帮助中心**

*代码块*

```Plain Text
GET /api/v1/user/help-center
```

**响应 data**：`List\&lt;HelpItemVO\&gt;`

### **7\.10 获取客服信息**

*代码块*

```Plain Text
GET /api/v1/user/customer-service
```

**响应 data**：`CustomerServiceVO`

**\-\-\-**

## **八、站点管理员包裹模块 \(****`AdminPackageController`****\)**

路径前缀：`/api/v1/admin` \| **需要认证，角色 ADMIN**

### **8\.1 获取站点包裹列表**

*代码块*

```Plain Text
GET /api/v1/admin/packages?status=IN_STOCK&page=1&size=100
```

**说明**：管理员工作台按状态筛选待审批、待骑手接单、派送中、已完成包裹。当前实现只返回当前管理员绑定站点 `station_id` 下的包裹；管理员不能复用用户包裹列表。`status=ALL` 表示不过滤状态。新注册站点管理员默认绑定“北京交通大学南门驿站”。

**响应 data**：`Page\&lt;PackageVO\&gt;`

### **8\.2 包裹入库（历史兼容）**

*代码块*

```Plain Text
POST /api/v1/admin/packages/{package_id}/inbound
```

**请求体**：

*代码块*

```Plain Text
{
  "shelf_number": "A-01-03"
}
```

**响应 data**：`Boolean`

**状态流转**：`PENDING\_INBOUND \-\&gt; IN\_STOCK`

**说明**：仅用于兼容旧流程。当前移动端主流程请使用 `POST /api/v1/admin/packages/{package_id}/approve` 完成审批并发布配送任务。

### **8\.2\.1 审批通过包裹并发布配送任务**

*代码块*

```Plain Text
POST /api/v1/admin/packages/{package_id}/approve
```

**说明**：这是当前移动端主流程的管理员审批接口。审批成功后，包裹状态从 `PENDING_INBOUND` 变为 `TASK_PUBLISHED`，并写入 `delivery_tasks` 表生成可抢任务。骑手随后可通过 `GET /api/v1/courier/tasks/available` 看到该任务。

**请求体**：

*代码块*

```Plain Text
{
  "reward_amount": 8.0
}
```

**响应 data**：`TaskVO`

**状态流转**：`PENDING_INBOUND -> TASK_PUBLISHED`，并创建 `delivery_tasks.status = AVAILABLE` 的任务。

### **8\.3 包裹出库（历史兼容）**

*代码块*

```Plain Text
POST /api/v1/admin/packages/{package_id}/outbound
```

**响应 data**：`Boolean`

**说明**：仅用于兼容旧流程。当前移动端主流程中，管理员审批通过后会直接生成骑手可抢任务。

### **8\.4 发布配送任务（历史兼容）**

*代码块*

```Plain Text
POST /api/v1/admin/tasks
```

**请求体**：

*代码块*

```Plain Text
{
  "package_id": "PKG-001",
  "reward_amount": 8.0
}
```

**响应 data**：`TaskVO`

**说明**：仅用于兼容旧流程。当前移动端主流程中，管理员使用审批接口一次性完成“审批通过 + 生成配送任务”。发布成功后会写入 `delivery_tasks` 表并返回 `task\_id`，后续骑手抢单、取件、核验取件码均使用 `task\_id`。同一个包裹已存在配送任务时，不重复创建任务。

**状态流转**：`IN\_STOCK \-\&gt; TASK\_PUBLISHED`

### **8\.5 获取站点统计**

*代码块*

```Plain Text
GET /api/v1/admin/station/statistics
```

**响应 data**：`StationStatisticsVO`

**\-\-\-**

## **九、骑手任务模块 \(****`CourierController`****\)**

路径前缀：`/api/v1/courier` \| **需要认证，角色 COURIER**

### **9\.1 获取可抢任务列表**

*代码块*

```Plain Text
GET /api/v1/courier/tasks/available?page=1&size=10
```

**说明**：获取站点管理员已发布、待抢单的配送任务。当前实现只返回 `delivery_tasks.status = AVAILABLE` 且 `courier_id IS NULL` 的任务大厅数据。骑手不能复用用户包裹列表。

**响应 data**：`Page\&lt;TaskVO\&gt;`

**说明**：只返回 `delivery_tasks.courier_id = 当前配送员ID` 的任务。`status=ALL` 表示不过滤状态；新注册配送员未抢单时，应返回空列表。

### **9\.2 获取我的配送任务**

*代码块*

```Plain Text
GET /api/v1/courier/tasks/mine?status=ASSIGNED&page=1&size=10
```

**响应 data**：`Page\&lt;TaskVO\&gt;`

### **9\.3 配送员抢单**

*代码块*

```Plain Text
POST /api/v1/courier/tasks/{task_id}/grab
```

**说明**：必须使用 `TaskVO\.task\_id`，不能使用 `package\_id`。

**响应 data**：`TaskVO`

**状态流转**：`AVAILABLE \-\&gt; ASSIGNED`，并同步 `packages.courier_id` 与 `packages.status = ASSIGNED`。

### **9\.4 确认取件**

*代码块*

```Plain Text
POST /api/v1/courier/tasks/{task_id}/pickup
```

**响应 data**：`TaskVO`

**说明**：仅允许当前任务所属配送员操作自己的任务。

**状态流转**：`ASSIGNED \-\&gt; DELIVERING`

### **9\.5 上传送达凭证**

*代码块*

```Plain Text
POST /api/v1/courier/tasks/{task_id}/deliver
```

**请求体**：

*代码块*

```Plain Text
{
  "deliver_image": "/uploads/proof.jpg",
  "remark": "已到达村民家门口"
}
```

**响应 data**：`TaskVO`

**说明**：仅允许当前任务所属配送员上传送达凭证。

### **9\.6 核验取件码并完成签收**

*代码块*

```Plain Text
POST /api/v1/courier/tasks/{task_id}/verify-pickup-code
```

**说明**：骑手上门后向用户索要取件码，核验成功后完成订单。该接口是当前移动端主签收动作。

**请求体**：

*代码块*

```Plain Text
{
  "pickup_code": "QJ25003"
}
```

**响应 data**：`TaskVO`

**状态流转**：`ASSIGNED/DELIVERING \-\&gt; COMPLETED`，并同步 `packages\.status \-\&gt; COMPLETED`，同时写入 `courier_earnings` 收益流水。

### **9\.7 查看收益**

*代码块*

```Plain Text
GET /api/v1/courier/earnings
```

**响应 data**：`EarningsVO`

### **9\.8 获取配送员资料**

*代码块*

```Plain Text
GET /api/v1/courier/profile
```

**响应 data**：`CourierProfileVO`

**\-\-\-**

## **十、乡镇资讯模块 \(****`ContentController`****\)**

路径前缀：`/api/v1/content` \| **需要认证**

### **10\.1 获取乡镇资讯**

*代码块*

```Plain Text
GET /api/v1/content/news?page=1&size=20
```

**响应 data**：`Page\&lt;NewsPostVO\&gt;`

*代码块*

```Plain Text
{
  "records": [
    {
      "id": 1,
      "title": "生鲜包裹优先配送",
      "content": "今天驿站生鲜包裹优先配送",
      "tag": "驿站动态",
      "author_name": "站点管理员",
      "station_name": "清河村中心驿站",
      "is_urgent": false,
      "publish_time": "2026-05-31T07:36:45.000Z",
      "likes": 15,
      "comments_count": 5
    }
  ],
  "total": 1,
  "size": 20,
  "current": 1,
  "pages": 1
}
```

**说明**：当前实现从 `news_posts` 表读取资讯，并聚合 `news_comments` 得到 `comments_count`。列表按发布时间倒序返回。

### **10\.2 发布乡镇资讯**

*代码块*

```Plain Text
POST /api/v1/content/news
```

**请求体**：

*代码块*

```Plain Text
{
  "title": "生鲜包裹优先配送",
  "content": "今天驿站生鲜包裹优先配送",
  "tag": "驿站动态",
  "is_urgent": false
}
```

**响应 data**：`NewsPostVO`

**说明**：发布成功后服务端会写入 `news_posts` 表，并返回入库后的 `NewsPostVO`。移动端应使用服务端返回的 `NewsPostVO` 更新列表，或重新调用 `GET /api/v1/content/news` 刷新列表。

### **10\.3 点赞资讯**

*代码块*

```Plain Text
POST /api/v1/content/news/{news_id}/like
```

**响应 data**：`Boolean`

### **10\.4 评论资讯**

*代码块*

```Plain Text
POST /api/v1/content/news/{news_id}/comments
```

**请求体**：

*代码块*

```Plain Text
{
  "content": "收到，谢谢提醒"
}
```

**响应 data**：`CommentVO`

**\-\-\-**

## **十一、驿站地图模块 \(****`StationController`****\)**

路径前缀：`/api/v1/stations` \| **需要认证**

### **11\.1 获取附近驿站**

*代码块*

```Plain Text
GET /api/v1/stations/nearby?lat=30.51&lng=114.31
```

**响应 data**：`List\&lt;StationVO\&gt;`

**说明**：当前实现从 `stations` 表读取真实站点。若传入 `lat/lng`，按距离从近到远返回；若未传坐标，按站点 ID 返回。当前已初始化北京交通大学南门及周边高校站点：

* 北京交通大学南门驿站
* 北京邮电大学西土城校区驿站
* 北京师范大学南门驿站
* 中央财经大学学院南路驿站
* 中央民族大学东门驿站
* 北京外国语大学东门驿站

### **11\.2 获取驿站详情**

*代码块*

```Plain Text
GET /api/v1/stations/{station_id}
```

**响应 data**：`StationVO`

**\-\-\-**

## **十二、文件上传模块 \(****`UploadController`****\)**

路径前缀：`/api/v1/upload` \| **需要认证**

### **12\.1 上传文件**

*代码块*

```Plain Text
POST /api/v1/upload
```

**请求格式**：`multipart/form\-data`

**响应 data**：`UploadVO`

**\-\-\-**

## **十三、后台用户管理模块 \(****`AdminUserController`****\)**

路径前缀：`/api/v1/admin/users` \| **需要认证**

### **13\.1 用户列表（分页\+搜索）**

*代码块*

```Plain Text
GET /api/v1/admin/users?page=1&size=10&keyword=xxx
```

**响应 data**：`Page\&lt;UserListVO\&gt;`

### **13\.2 创建用户**

*代码块*

```Plain Text
POST /api/v1/admin/users
```

**请求体**：`CreateUserDTO`

**响应 data**：创建后的 `Users` 对象（`passwordHash` 字段置空）

### **13\.3 更新用户**

*代码块*

```Plain Text
PUT /api/v1/admin/users/{id}
```

**请求体**：`Users` 实体

**响应 data**：更新后的 `Users` 对象

### **13\.4 删除用户**

*代码块*

```Plain Text
DELETE /api/v1/admin/users/{id}
```

**响应 data**：`null`

**\-\-\-**

## **十四、核心 VO/DTO 字段定义**

### **14\.1 LoginResponseVO**

### **14\.2 UserVO**

### **14\.3 PackageVO**

### **14\.4 TaskVO**

### **14\.5 NewsPostVO**

*代码块*

```Plain Text
{
  "id": 1,
  "title": "生鲜包裹优先配送",
  "content": "今天驿站生鲜包裹优先配送",
  "tag": "驿站动态",
  "author_name": "站点管理员",
  "station_name": "清河村中心驿站",
  "is_urgent": false,
  "publish_time": "2026-05-31T07:36:45.000Z",
  "likes": 15,
  "comments_count": 5
}
```

**字段说明：**
- `id`：资讯主键，对应 `news_posts.id`。
- `author_name`：发布人展示名，优先取村民昵称或管理员真实姓名。
- `station_name`：关联驿站名称。
- `publish_time`：发布时间，对应 `news_posts.created_at`。
- `comments_count`：评论数量，聚合 `news_comments` 正常状态评论。

**\-\-\-**

## **十五、移动端功能与接口映射**

**\-\-\-**
